# zoesTM local/dev defaults
ENVIRONMENT=development
PORT=8000
DB_PATH=apps/backend/data/zoestm.db
WEBHOOK_SIGNING_KEY=dev-only-change-me

# Set to 1 for local testing without scope headers
ZOESTM_DEV_AUTH=1

# Optional global auth enforcement
ZOESTM_ENFORCE_AUTH=0
